class Counter {
    public static const seed: int = 6;
    private let value: int;

    func Counter(initial: int) -> void {
        this.value = initial;
    }

    func get() -> int {
        return this.value;
    }
}

let counter: Counter = new Counter(Counter.seed + 1);
print(counter.get());
