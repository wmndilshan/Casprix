// TinyStories Preprocessing
// Tokenize and create binary dataset

// For now, using simple approach until compiler fully supports external bindings
// We'll use C for preprocessing and Casprix for training

func main() -> void {
    print("=== TinyStories Preprocessing ===")
    print("")
    print("This script tokenizes TinyStories and creates binary dataset")
    print("")

    // Configuration
    let vocab_size: int = 256          // Byte-level for now
    let seq_length: int = 128          // Sequence length
    let input_path: string = "data/tinystories.txt"
    let output_path: string = "data/tinystories_tokenized.bin"

    print("Configuration:")
    print("  Vocabulary size: " + vocab_size)
    print("  Sequence length: " + seq_length)
    print("  Input: " + input_path)
    print("  Output: " + output_path)
    print("")

    // In full implementation with compiler support:
    // 1. Load tokenizer or train new one
    // 2. Read text file
    // 3. Tokenize all stories
    // 4. Create dataset
    // 5. Save binary format

    print("Note: Preprocessing currently done via C utilities")
    print("Run: preprocess_tinystories.exe")
}

main()
