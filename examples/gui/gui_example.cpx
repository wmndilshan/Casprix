// Example: GUI operations
import "lib/gui/gui.cpx";

print("GUI Operations Demo");
print("===================");

// Note: These functions are placeholders until runtime is fully integrated
// In a real implementation, you would use:
// window: int = WindowCreate("My Window", 800, 600);
// WindowShow(window);
// WindowProcessEvents(window);

print("GUI operations module loaded");
print("Functions available:");
print("  - WindowCreate(title, width, height)");
print("  - WindowShow(handle)");
print("  - WindowHide(handle)");
print("  - WindowClose(handle)");
print("  - WindowSetTitle(handle, title)");
print("  - ButtonCreate(parent, text, x, y, width, height)");
print("  - LabelCreate(parent, text, x, y)");
print("  - TextInputCreate(parent, x, y, width, height)");
print("  - WindowProcessEvents(handle)");
print("  - MessageBoxShow(title, message)");



