let base: int = 10;

print((|x: int| => x + base)(5));

print((|x: int| -> int {
    let y: int = base + x;
    return y;
})(7));

func add_with_bias(bias: int, x: int) -> int {
    return (|v: int| => v + bias)(x);
}

print(add_with_bias(4, 6));
