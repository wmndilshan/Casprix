<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml for a pure Skia/NativeActivity app.

    NO <activity> with a layout.
    NO Java UI.
    Just android.app.NativeActivity loading libMainActivity.so.

    This is exactly how Flutter-release APKs work:
      - android.app.NativeActivity
      - android.app.lib_name = "MainActivity"
      - libMainActivity.so exports ANativeActivity_onCreate()
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.casprix.app"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk
        android:minSdkVersion="24"
        android:targetSdkVersion="34" />

    <!-- OpenGL ES 3.0 required (Skia GPU backend) -->
    <uses-feature android:glEsVersion="0x00030000" android:required="true" />

    <application
        android:label="@string/app_name"
        android:hasCode="false"
        android:allowBackup="false"
        android:supportsRtl="true">

        <!--
            android:hasCode="false" = no Java/Kotlin code at all.
            The entire app lives in libMainActivity.so.
        -->

        <activity
            android:name="android.app.NativeActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:configChanges="orientation|keyboardHidden|screenSize|screenLayout|uiMode"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar"
            android:windowSoftInputMode="adjustResize">

            <!--
                This tells NativeActivity which .so to load.
                Must match the OUTPUT_NAME in CMakeLists.txt.
            -->
            <meta-data
                android:name="android.app.lib_name"
                android:value="MainActivity" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
